services:
  reverse-proxy:
    image: nginx:alpine
    container_name: dockerbung-reverse-proxy
    depends_on:
      web:
        condition: service_healthy
    ports:
      - "80:80"
    volumes:
      - ./proxy/nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
